language: node_js
node_js:
  - "10"

cache:
  directories:
    - "node_modules"

install:
  - npm install

script:
  - npm run build

deploy:
  - provider: npm
    email: $NPM_API_EMAIL
    api_key: $NPM_API_TOKEN
    skip-cleanup: true
    on:
      tags: true
